name: orbit_state
description: >
  A tiny, Pinia-style state management library for Flutter. Zero
  dependencies, built entirely on ChangeNotifier + AnimatedBuilder.
version: 0.2.0
homepage: https://github.com/yourname/orbit

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
